#include "main.h"
/**
* mul - multiples of two integers
* @j: first number to multiply
* @k: second number multiplied with the first
* Return: the result of multiplying j and k
*/
int mul(int j, int k)
{
return (j * k);
}
