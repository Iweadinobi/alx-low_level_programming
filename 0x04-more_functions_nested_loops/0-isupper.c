#include "main.h"
/**
* _isupper - check if a letter is upper case
* @g: the letter to be checked
* Return: 1 for upper case letter, 0 for others
*/
int _isupper(int g)
{
if (g >= 65 && g <= 90)
{
return (1);
}
return (0);
}
